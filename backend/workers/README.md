# Backend Workers

This folder is reserved for future background workers such as cleanup, indexing, scheduled maintenance, or moderation jobs.

The active notification Worker currently lives in:

```text
backend/cloudflare-worker/
```
