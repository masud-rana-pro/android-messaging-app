# ContactMe Logo

This folder is reserved for logo source files, experiments, and export notes.

The Android app currently uses bitmap logo assets from:

```text
apps/ContactMe/app/src/main/res/drawable-nodpi/
```

Keep editable source files here when logo iterations are created outside Android resources.
