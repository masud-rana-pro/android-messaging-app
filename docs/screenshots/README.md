# Screenshots

This folder is reserved for release screenshots, QA captures, and portfolio images.

Suggested structure:

```text
android/
  auth/
  chats/
  calls/
  settings/
```

Avoid committing screenshots that contain private user data, phone numbers, tokens, or real conversations.
